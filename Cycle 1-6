#include <stdio.h>
main()
{
	int a,b,c,d=0;
	printf("Enter The Number To Sum it's digits:");
	scanf("%d",&a);
	b=a;
	while(a!=0)
	{
		d+=a%10;
		a/=10;
	}
	printf("The Sum of the Digits of %d is %d",b,d);
}
